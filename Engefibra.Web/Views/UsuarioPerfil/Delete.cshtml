@model Engefibra.Data.Models.UsuarioPerfil
@{
    ViewBag.Title = "Remover";
}
<h2>Remover vinculo de Perfil</h2>
<div>
    <h4>Você tem certeza que deseja fazer isso?</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Perfil.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Perfil.Nome)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Usuario.Pessoa.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Usuario.Pessoa.Nome)
        </dd>

    </dl>

    @using (Html.BeginForm()) 
    {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Remover" class="btn btn-danger" /> |
            @Html.ActionLink("Voltar", "Index", null, new { @class = "btn btn-primary" })
        </div>
    }
</div>
